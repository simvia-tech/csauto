# Select the code_aster solver and run it with Docker
solver = "code_aster"
runtime = "docker"
docker_image = "simvia/code_aster:17.4.0"

# Web UI binding
host = "127.0.0.1"
port = 8000

# Required when host is not 127.0.0.1 or localhost
[api]
token = "your-secret-token"
